# 代码块与引用

## 带语言的代码块

```rust
fn main() {
    let docs = vec!["标题", "列表", "代码"];
    for doc in docs {
        println!("处理: {}", doc);
    }
}
```

```python
def extract(url: str) -> str:
    """提取文档正文"""
    return fetch(url).markdown
```

```json
{
  "ok": true,
  "data": { "title": "测试" }
}
```

## 无语言标记的代码块

```
plain text block
second line
```

## 行内代码

调用 `lark-cli docs +fetch` 获取内容，参数 `--doc-format markdown`。

## 引用

> 这是一段引用文本。  
> 引用可以有多行。

> 嵌套引用测试

> 这是嵌套在引用内部的引用。

## 分割线

上方内容

---

下方内容

## 转义与特殊符号

尖括号：`<div>` 与 `</div>`

和号：A & B

星号与下划线：a \* b \_ c

反斜杠路径：C:\Users\test\docs

## 新增代码块：JavaScript

```js
const docs = ["标题", "列表", "代码"];
docs.forEach(doc => console.log(`处理: ${doc}`));
```

## 新增代码块：SQL

```sql
SELECT title, revision_id
FROM documents
WHERE title LIKE "%测试%";
```

## 嵌套引用测试

> 外层引用文本

> 内层嵌套引用

新增验证标记：APPENDED-CODE-REF-CHECK