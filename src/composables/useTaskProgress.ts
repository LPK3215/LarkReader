// ============================================================================
// src/composables/useTaskProgress.ts —— 任务进度轮询（结构占位）
//
// 职责（M2 填充）：
//   给定 taskId，以约定间隔轮询 api/task.ts 的 get_progress，
//   暴露响应式 progress / phase / error / stop()，并注入 stores/task.ts。
//   顺带处理 cancel 后的轮询终止与 get_task_result 收尾。
//
// 说明：当前无依赖，仅占位；填充后再删除本注释并引入 api/task.ts 与 pinia。
// ============================================================================

export {};
