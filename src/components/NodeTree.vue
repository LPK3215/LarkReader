<!--
  NodeTree —— 知识库可勾选树（结构占位）
  填充时机：M2。输入：api/types.ts 的 WikiNode 树。
  支持：勾选/全选/展开、按 obj_type 图标区分（doc/sheet/bitable/file/folder）、
  输出选中节点 token 列表给 stores/task.ts 的 selectedTokens。
  说明：UI 库（Naive UI / Element Plus）定稿后，树的实现只收敛在本组件内部。
-->
<script setup lang="ts">
// TODO(M2): 树渲染与勾选模型（children 需由 has_child 惰性加载或整树传入）。
</script>

<template>
  <div />
</template>
