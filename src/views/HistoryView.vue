<!--
  HistoryView —— 任务历史页（结构占位）
  填充时机：M2（与工作台同批或略后）。
  职责：api/task.ts list_task_history 渲染历史任务（成功/失败/路径），
  支持 dismiss 单条、open_output_dir 打开产物目录、失败重试入口（如需要）。
  路由：/history。
-->
<script setup lang="ts">
// TODO(M2): 历史列表 + 操作按钮。
</script>

<template>
  <div />
</template>
